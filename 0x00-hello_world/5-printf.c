#include <stdio.h>

/**
 * main - Print and compile
 *
 * Return: Always 0 (Success)
 */
gcc -Wall 5-printf.c -o printf

int main(void)
{
	printf("\n"with proper grammar, but the outcome is a piece of art,");
	return (0)
}
