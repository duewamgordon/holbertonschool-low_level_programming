#!/bin/bash
#include <stdio.h>
/**
 * print - print "with proper grammar, but the outcome is a piece of art"
 * Return: (0).
 **/

int main()
{
	printf("\"with proper grammar, but the outcome is a piece of art");
	Return(0);
}
