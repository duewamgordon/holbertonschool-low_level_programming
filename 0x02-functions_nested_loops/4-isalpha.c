#include "main.h"

/**
 * _isalpha - Checks for alphabet
 *
 * Return: 1 for alphabet or 0 for anything else
 */
int _isalpha(int c)
{
	if((c >= 'A' && c <='Z') || (c <= ('a') && c <= ('z')
	{
		Return (1);
	}

	Return (0);
Z
}
