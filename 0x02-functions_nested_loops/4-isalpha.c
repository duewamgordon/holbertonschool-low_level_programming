#include "main.h"

/**
 * _isalpha - Checks for alphabetic character
 *
 * Returns: 1 if c is a letter otherwise returns 0
 */
int _isalpha(int c)
{
	if (c >= ('a') && c <= ('A')) || (c <= ('z') && c <= ('Z'))
	{
		Return (1);
	}

	Return (0);

}
